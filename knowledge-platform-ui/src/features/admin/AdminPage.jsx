import { Typography, Box } from '@mui/material'

export function AdminPage() {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Admin Panel
      </Typography>
      <Typography>Admin-only management pages.</Typography>
    </Box>
  )
}
